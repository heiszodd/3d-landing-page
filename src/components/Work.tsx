import { useState, useCallback } from "react";
import "./styles/Work.css";
import WorkImage from "./WorkImage";
import { MdArrowBack, MdArrowForward } from "react-icons/md";
const projects = [{ title: "ZSP Overview / Terminal", category: "High-level algorithmic execution & market microstructure system", tools: "TypeScript, Python, Systems Architecture, Algorithmic Execution, Quantitative Analysis", image: "/images/callhq.png", link: "https://github.com/heiszodd" }];
const Work = () => { const [currentIndex,setCurrentIndex]=useState(0); const [isAnimating,setIsAnimating]=useState(false); const goToSlide=useCallback((index:number)=>{if(isAnimating)return;setIsAnimating(true);setCurrentIndex(index);setTimeout(()=>setIsAnimating(false),500)},[isAnimating]); const prev=()=>goToSlide(currentIndex===0?projects.length-1:currentIndex-1); const next=()=>goToSlide(currentIndex===projects.length-1?0:currentIndex+1); return <div className="work-section" id="work"><div className="work-container section-container"><h2>Selected <span>Work</span></h2><div className="carousel-wrapper"><button className="carousel-arrow carousel-arrow-left" onClick={prev} aria-label="Previous project" data-cursor="disable"><MdArrowBack/></button><button className="carousel-arrow carousel-arrow-right" onClick={next} aria-label="Next project" data-cursor="disable"><MdArrowForward/></button><div className="carousel-track-container"><div className="carousel-track" style={{transform:`translateX(-${currentIndex*100}%)`}}><div className="carousel-slide"><div className="carousel-content"><div className="carousel-info"><div className="carousel-number"><h3>01</h3></div><div className="carousel-details"><h4>{projects[0].title}</h4><p className="carousel-category">{projects[0].category}</p><div className="carousel-tools"><span className="tools-label">Stack &amp; Focus</span><p>{projects[0].tools}</p><p>This is a high-level overview; proprietary internal model rules, formulas, exact setups, and trade secrets are not disclosed.</p></div></div></div><div className="carousel-image-wrapper"><WorkImage image={projects[0].image} alt={projects[0].title} link={projects[0].link}/></div></div></div></div></div><div className="carousel-dots"><button className="carousel-dot carousel-dot-active" aria-label="Go to project 1" data-cursor="disable"/></div></div></div></div>; };
export default Work;
